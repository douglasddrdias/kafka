spring:
  application:
    name: kafka
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: io.confluent.kafka.serializers.KafkaAvroSerializer
      properties:
        schema.registry.url: http://localhost:8081
        auto.register.schemas: true

    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: io.confluent.kafka.serializers.KafkaAvroDeserializer
      properties:
        schema.registry.url: http://localhost:8081
        specific.avro.reader: true
      group-id: starwars-consumer-group
starwars:
  topic: starwars-topic
swapi:
  url: https://swapi.dev/api/people/
image:
  url: https://starwars-databank-server.vercel.app/api/v1/characters/name/
quote:
  url: https://starwars-quotes.herokuapp.com/api/quotes
logging:
  level:
    com.br.kafka.services.external: DEBUG
